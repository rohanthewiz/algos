package main

// This is just for current algo checking
// Eventually use these to create tests under each algo package
func main() {
	// run `go test -v ./...`
	// For tests that do not run, change to package directory and run
	// `go test`
}
